spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/librarydb?options=-c%20TimeZone%3DUTC
    username: postgres
    password: changeit
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:easyenglish515253@gmail.com}
    password: ${MAIL_PASSWORD:yrfkjncxirhrfoqw}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: ${MAIL_HOST:smtp.gmail.com}

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework: INFO

jwt:
  secret: ${JWT_SECRET:Zm9yLWhTMjU2LXdlLW5lZWQtMzItYnl0ZXMhIQ==JKAFlksdf}
  expiration-ms: 86400000

session:
  ttl-hours: 24
  secure-cookie: false

email:
  smtp:
    enabled: ${MAIL_ENABLED:true}

  from-name: Technical Literature Library
  verification:
    ttl-minutes: 15
    resend-seconds: 60

# Локальний скид пароля admin (видаліть або закоментуйте після першого входу)
library:
  bootstrap:
    reset-admin-password: admin
